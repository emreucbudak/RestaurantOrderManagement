<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="RestaurantOrderManagement.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:RestaurantOrderManagement"
    Title="" Shell.NavBarIsVisible="False"
    xmlns:pages="clr-namespace:RestaurantOrderManagement.Pages">

    <ShellContent
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />
    <ShellContent ContentTemplate="{DataTemplate pages:RestaurantManagerPanel}"
                  Route="RestaurantManager"/>
    <ShellContent ContentTemplate="{DataTemplate pages:WaiterPanel}"
                  Route="WaiterPanel"/>
    <ShellContent ContentTemplate="{DataTemplate pages:DeskManagement}"
                  Route="DeskManagement"/>
    <ShellContent ContentTemplate="{DataTemplate pages:ProductManagement}"
                  Route="ProductManagement"/>
    <ShellContent ContentTemplate="{DataTemplate pages:AddOrders}"
                  Route="AddOrders"/>
    <ShellContent ContentTemplate="{DataTemplate pages:OrderListPage}"
                  Route="OrderList"/>
    <ShellContent ContentTemplate="{DataTemplate pages:WaiterManagementPage}"
                  Route="WaiterManagement"/>
    
</Shell>
